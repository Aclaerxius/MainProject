{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "C/C++ Runner: Debug Session",
      "type": "lldb",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "program": "${workspaceFolder}/build/cryptoticker",
      "preLaunchTask": "build",
      "args": [],
    },
    {
      "name": "C/C++ Runner: Debug Session With Arguments",
      "type": "lldb",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "program": "${workspaceFolder}/build/cryptoticker",
      "preLaunchTask": "build",
      "args": [
        "dfdf",
        "--dfdfd"
      ],
    }
  ]
}
